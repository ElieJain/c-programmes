#include <stdio.h>

int main()
{   int x, flag=0,i,t1=0, t2=1; 
    printf("please enter number of terms:");
    scanf(%d,x)
for (i = 1; i <= n; ++i) {
        printf("%d, ", t1);
        nextTerm = t1 + t2;
        t1 = t2;
        t2 = nextTerm;
    return 0;
}
